# /etc/measurement-conversion-api/app.env
# Copy to /etc/measurement-conversion-api/app.env and `chmod 600`.
# Do NOT commit real credentials.

SERVER_PORT=8080
DB_URL=jdbc:oracle:thin:@//127.0.0.1:1521/XEPDB1
DB_USER=measurement
DB_PASSWORD=replace_me_strong_password
LOG_DIR=/var/log/measurement-conversion-api
